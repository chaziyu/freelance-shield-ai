# Synthetic local configuration only. Never commit real secrets or client data.
GOOGLE_API_KEY=
GOOGLE_ADK_MODEL=gemini-2.5-flash
FREELANCE_SHIELD_DB=/app/data/freelance_shield.db
# Set to 1 only for deterministic local tests that intentionally bypass ADK.
FREELANCE_SHIELD_ALLOW_LOCAL_WORKFLOW=0
APP_ENV=development
